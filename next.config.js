// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Se usi ISR con WordPress
  images: {
    domains: ['www.ventoadv.it', 'placehold.co'],
  },
};

export default nextConfig;